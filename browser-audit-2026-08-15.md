# AI Video 真实浏览器视觉巡检

日期：2026-08-15  
环境：`http://127.0.0.1:4173/`，开发演示数据  
截图目录：`C:\Users\16691\AppData\Local\Temp\ai-video-browser-audit-20260815`

## 覆盖范围

- 50 张真实浏览器截图。
- 视口：1440×900、988×720、390×844。
- Studio：7 个路由（首页、创作、素材、作品、发布、历史、设置）。
- Company：5 个模块（经营概览、成员与角色、模型与功能、使用报表、余额流水）。
- Operations：8 个模块。
- 交互状态：首页生成器展开态、新建发布弹窗、成员权限抽屉、运营异常抽屉。
- 皮肤：纯白完整矩阵，雾灰与暖米桌面烟雾测试。

## 总体结论

- P0：0。
- P1：4。
- P2：3。
- 50 个状态均未出现文档级横向溢出。
- 未发现小于 12px 的可见普通文本。
- 桌面端三套工作区已经形成较统一的浅色视觉系统；主要剩余问题集中在移动端数据排版、滚动所有权和中间断点工具栏。

## P1 问题

### 1. Company 成员表在 390px 出现字段重叠

角色说明、状态和操作列进入同一视觉区域，员工权限容易被误读，操作按钮也失去明确归属。

证据：[30-company-members-390x844.png](C:/Users/16691/AppData/Local/Temp/ai-video-browser-audit-20260815/30-company-members-390x844.png)

建议：在手机断点将成员表切换为真正的记录布局；每条记录按“成员 → 角色/权限 → 状态 → 操作”排布，不删除任何字段或操作。

### 2. Studio 历史记录标题被压缩为 34px

手机端任务标题只显示为“产…”，状态占据了标题行主体。最终路由层中的两列网格把标题落入了固定 34px 列。

证据：[39-studio-history-390x844.png](C:/Users/16691/AppData/Local/Temp/ai-video-browser-audit-20260815/39-studio-history-390x844.png)  
源码关联：`src/design-system/studio-routes.css` 约 1511 行。

建议：让标题占满正文列，状态与费用进入独立行；保留任务状态、发起人、公司、模型、参数和动作。

### 3. Studio 次级页在手机端存在双重纵向滚动

文档本身可滚，同时 `.main-canvas` 也拥有独立滚动，页面出现两条垂直滚动轨道。该问题会造成滚动位置跳变、底部导航附近手势归属不清。

实测：390×844 的素材页中，document 高度约 1685px，而 `.main-canvas` 同时为 `overflow:auto`、内容高度约 1677px。

建议：Studio 手机端仍由 `.main-canvas` 作为唯一内容滚动面，页面根节点不要再次参与纵向滚动；Company 与 Operations 的自然文档流保持不变。

### 4. Operations 在 988px 将“生产环境”拆成竖排

时间范围、环境、刷新和异常入口在中间断点争抢单行空间，环境标签被压成逐字换行。

证据：[45-ops-task-operations-988x720.png](C:/Users/16691/AppData/Local/Temp/ai-video-browser-audit-20260815/45-ops-task-operations-988x720.png)

建议：为环境控件保留最小宽度和 `white-space: nowrap`，或在该断点将环境与刷新移动到第二行；不要缩小字号。

## P2 问题

### 5. Studio 手机顶栏出现两个空白窄条

演示账号切换器在 390px 被压到约 16px 宽，形成两个像空白按钮的竖条，用户无法识别其用途。

证据：[34-studio-home-390x844.png](C:/Users/16691/AppData/Local/Temp/ai-video-browser-audit-20260815/34-studio-home-390x844.png)

建议：手机端将演示身份合并为一个明确的账号入口或可读下拉，不能继续依赖被 flex 压缩的原生 select。

### 6. 44px 手机触控合同未在三套工作区一致生效

巡检脚本在多数 390px 页面发现 32–40px 的可见按钮或 select。典型包括 Studio 顶栏、Company 行内操作、Operations 模块导航和筛选控件。

建议：按语义统一为 compact 32、default 40、mobile touch 44；手机端的独立可点击动作必须达到 44px，复选框可由更大的 label 承担点击区域。

### 7. Company 与 Operations 表格内部仍有 12px 文本

表格正文基础字号已是 13px，但 role chip、行内动作、`small` 元数据和审计差异说明仍被局部规则压回 12px。涉及 Company 成员/报表和多张 Operations 表。

建议：表格中的业务值、状态和操作统一使用 `--control-table-size`；仅真正的辅助注释允许 12px，并确保对比度与行高充足。

## 通过项

- Studio 首页生成器在桌面浮动、创作页参与布局；巡检未再发现生成器覆盖正文动作。
- 生成器展开态在 390px 可内部滚动，费用与开始生成按钮保持可达。
- 发布弹窗、成员权限抽屉和运营异常抽屉均完整落在视口内，正文可滚、底部动作可见。
- Operations 的宽表和流程图把横向滚动限制在内部容器，没有扩散为页面级横向溢出。
- 雾灰与暖米皮肤没有回流为暗色主题；三个工作区的主要表面与语义状态保持一致。

## 说明

本轮仅做真实浏览器巡检、截图和报告，没有修改产品代码。截图可以证明当前视觉与几何状态，但不能单独证明完整的键盘、读屏或生产数据行为。
