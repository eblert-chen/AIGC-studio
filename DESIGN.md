# 旭天 AI VIDEO 设计系统

本文件记录当前产品已实现的视觉与交互契约。它服务于 Studio、Company、Operations 与 Auth；任何后续页面都应在这套系统中演进，而不是重新发明一套局部风格。

## 设计方向

设计方向名为 **Daylight Conform Desk（日光精编台）**。

- 纯白或近白的连续工作表面是默认画布。
- 科技感来自精确的轴线、真实媒体、实时状态、任务证据、清晰数字与可靠反馈，不来自黑底、紫色渐变、玻璃拟态或霓虹外发光。
- 结构主要由字体、留白、细分隔线和信息层级建立；避免卡片套卡片、指标墙、无意义徽章和装饰性 AI 图案。
- 功能密度可高，但认知负担必须通过分组、渐进披露和稳定位置来控制。
- `paper` 是视觉源；`mist` 与 `warm` 只改变语义颜色，不改变布局、密度、权限或数据。

## 品牌

- 产品名称：旭天 / AI VIDEO。
- 品牌资产位于 `public/brand/`，产品界面统一使用 `BrandLogo`。
- 桌面优先使用透明底石墨色字标；紧凑与手机空间使用 X 符号。
- 不再使用 Sparkle、FilmSlate、ShieldCheck、字母头像或“影创 Verse”旧品牌。
- 浅色页面不得把深色原稿背景直接当作 Logo 容器。

## CSS 架构

浏览器只有一个入口：`src/design-system/index.css`。

级联顺序固定为：

1. `system.tokens`
2. `system.foundation`
3. `system.controls`
4. `system.shells`
5. `system.routes`

旧文件 `styles.css`、`community.css`、`creation-hub.css`、`publishing.css`、`light-theme.css` 与 `admin/operations-console.css` 不再进入运行时。不要通过重新导入它们或追加高特异性补丁解决问题。新组件应消费语义 token；迁移页面时删除退休声明。

## 字体与排版

- 字体栈：`Manrope Variable` + `Noto Sans SC Variable`，随后才是系统回退。
- 代码、ID 与不可变证据使用 `--font-mono`。
- 普通可见文案不得低于 12px；表格正文不得低于 13px。
- 仅使用 400、500、600、700 四档字重。
- 页面大标题通常为 28–38px；路由标题、段落、caption 通过共享 token 表达，不写零散的 9/10/11px。
- 中英文、数字和金额必须共享基线；金额和 ID 不应因为备用字体产生明显宽度跳动。

## 空间、尺寸与形状

- 基础间距遵循 4px 网格：4、8、12、16、24、32、40、48、64。
- 控件高度只使用三档：32px 紧凑、40px 默认、44px 触控/主操作。
- 手机端所有真实操作目标至少 44px；图标本身可以更小，但命中区域不能缩小。
- 圆角只使用语义档：6px 控件、10px 表面、14px 浮层、999px 状态胶囊。
- 默认表面使用细边线与极轻阴影；禁止多层悬浮卡片、厚重 glow 和任意 16/20/24px 大圆角。
- 焦点使用统一的高对比 focus ring，不能以移除 outline 代替设计。

## 色彩与状态

- 石墨色承担主要文字；矿物绿承担选择、成功和主要动作。
- warning、danger、success、info 必须使用语义 token，不得以同一种绿色表现所有状态。
- 状态色不能单独传递含义；必须同时提供文字、图标或形状。
- 3px 语义边线只允许用于高风险入口、明确演示告知和错误/警告状态，不能变成装饰性侧边标签。
- 数据缺失必须显示“未提供”“不可用”“待核验”等真实状态，不能伪造为 0 或空成功态。

## 三类产品壳层

三种导航模型是有意差异，不应强行统一为同一种菜单。

### Studio

- 桌面使用 56px 顶栏 + 80px 图标侧栏。
- 首页为媒体主导的编辑式灵感舞台；真实项目图片承担视觉焦点。
- 创作页使用记录区 + 能力驱动生成工作台。创作工作台必须参与布局，不能覆盖最后一项任务或操作。
- 素材、作品、发布、历史、设置共享一致页头、工具栏、内容宽度和状态语言。
- 首页生成器可浮动，但必须有真实 clearance；任务条与底部导航不能重叠。
- 个人工作区复用相同七条路由，但 points、个人任务与企业 cents/钱包严格隔离。

### Company

- 桌面保留文字侧栏，强调连续账本、成员、模型、报表与余额，而不是 KPI 卡片墙。
- 导航按服务端权限裁剪；无权模块不得先渲染再用 CSS 隐藏。
- 表格、摘要带与抽屉使用统一密度；成员权限三态与并发快照语义不得被“简化”为普通开关。
- 多公司上下文必须显示真实授权公司，演示模式不得伪造额外公司。

### Operations

- 保留横向模块导航和高密度连续 Operations Canvas。
- 任务状态流与异常队列在 1280px 桌面并列；<=1180px 才折叠为单列。
- 图表必须提供键盘可读的数值表或详情；图形不能成为唯一证据。
- 权益预览、Relay fencing、unknown submission、callback DLQ 与原生控制台入口保留原安全流程。
- 运营模块可以密集，但不得出现未经核验的假零值、`null`/`undefined` 原样输出或死操作。

### Auth

- 正式认证页把身份、范围与权限边界解释清楚，不在客户前端收集密码、MFA 或通行密钥。
- 登录、邀请、回调、失效与退出状态必须明确区分 loading、warning、success 和 fail-closed。
- 登录主操作在手机端至少 48px；皮肤控件至少 44px。

## 手机组合

手机端不是压缩桌面。

- 390px 与 320px 都是强制验收宽度。
- Studio 使用底部七路由导航；320px 每项仍保留 44px 命中区域，不缩字换空间。
- 首页优先主视觉与启动动作；短屏生成器收敛为可展开 launcher。
- 创作页优先当前记录、核心筛选、生成动作与任务状态；次级筛选可横向滚动或渐进披露。
- Company 使用两行命令区、横向模块栏和记录式移动表格，所有字段与授权动作仍可达。
- Operations 使用两行命令区、可操作的模块滚动控制、sticky 关键操作与明确横滑提示。
- 抽屉使用 `dvh` 与 safe-area，标题、滚动正文和 footer 始终可见。
- 页面本身不得横向溢出；只有明确标注的表格、流程或素材轨可以内部横滑。

## 状态与真实性

每个数据区至少考虑：loading、empty、error、unauthorized、unavailable 与正常数据。

- 演示模式必须显式标注“演示”，且只在开发构建由 `VITE_ENABLE_DEMO=true` 开启。
- 生产构建即使误传演示标志也必须 fail closed。
- 首页素材必须说明它们是项目演示素材，不得伪装为真实社区热度。
- 发布账号、发布任务、Relay 渠道和支付不得用 mock 冒充生产能力。
- 签发预览/下载 URL 不等于完成下载；UI 只展示后端证据允许的状态。
- 任务 `timed_out`、`reconciliation_required` 与未知状态不得显示为排队中。

## 可访问性与交互

- 所有 tab 使用 roving tabindex，并支持方向键、Home、End。
- dialog/drawer 支持 Escape、Tab/Shift+Tab 循环和关闭后焦点恢复。
- 图标按钮必须有可访问名称；带可见文字的按钮不得被通用规则压成方形。
- 隐藏文件输入不能进入 Tab 顺序；可见按钮仍触发同一真实上传逻辑。
- reduced-motion 下移除非必要缩放与位移动效。
- hover 不是唯一发现方式；横向导航和表格必须有键盘与视觉提示。

## 后续贡献规则

1. 先复用已有 token、control 与 route anatomy，再新增局部样式。
2. 视觉重构只能改表现层；权限、计费、幂等、钱包、Relay 与发布安全语义必须保持服务端权威。
3. 不新增假入口、假指标、假数据或无法完成的按钮。
4. 新 route CSS 进入 `system.routes`，不得重新导入退休文件。
5. 不写低于字号下限的文案，不创建第四种控件高度或第五种圆角。
6. 完成后运行 `npm test`、`npm run build`、`npm run test:sites`，并用真实浏览器检查桌面、390px 与 320px。
7. 每个角色都要验证正向和负向权限；页面可见性不替代接口鉴权。

## 最小浏览器矩阵

- 1440×900：Studio 七路由、Company 五模块、Operations 九模块。
- 1280×800：Operations 任务状态流与异常队列并列。
- 1024×700：短桌面滚动、生成工作台、发布对话框和管理抽屉。
- 390×844：三类 shell 完整流程与全部 permitted actions。
- 320×640：Studio 首页/创作/账号、Company 成员/企业目录、Operations 任务/审计、Auth 登录。
- 键盘：tabs、生成器、结果弹窗、发布弹窗、管理抽屉与 Operations 高风险流程。
